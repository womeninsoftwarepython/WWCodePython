import tkinter as tk    # importing the tkinter module

# create an instance of the Tk()
root = tk.Tk()

# create a label to display text
label = tk.Label(root, text="Women Who Code Python")
label.pack()    # pack() places the label in the window

root.mainloop() # keeps the window open for infinite duration
